# Pipeline con YAML inválido
# Escenario: Errores de sintaxis

trigger:
  branches:
    include:
      - main

pool
  vmImage: 'ubuntu-latest'  # PROBLEMA: falta ':'

stages:
- stage: Build
  jobs:
  - job: BuildJob
    steps:
      - script: npm ci
        displayName: 'Install'
      - script: npm run build
      displayName: 'Build'  # PROBLEMA: indentación incorrecta

# EXPECTED: Parse error
